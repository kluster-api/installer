apiVersion: v1
kind: Service
metadata:
  name: {{ include "azure-credential-manager.webhookServiceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "azure-credential-manager.labels" . | nindent 4 }}
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    {{- include "azure-credential-manager.labels" . | nindent 4 }}
