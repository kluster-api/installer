apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "dockermachine-operator.fullname" . }}
  labels:
    {{- include "dockermachine-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "dockermachine-operator.fullname" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "dockermachine-operator.fullname" . }}
  namespace: {{ .Release.Namespace }}
